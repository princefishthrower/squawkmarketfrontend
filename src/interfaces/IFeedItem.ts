// corresponds to golang backend type "Squawk"
export interface ISquawk {
  created_at: string;
  link: string;
  symbols: string;
  feed: string;
  squawk: string;
  mp3data: string;
  url: string;
}
